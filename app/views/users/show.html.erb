<% if flash[:notice] %>
  <p id = "notice" >
    <%= flash[:notice] %>
  </p>
<% end %>

<h1>User info</h1>
<% if @user.image.attached? %>
    <%= image_tag user.image, size: "100x100" %>
<% else %>
    <%= image_tag 'no_image', size: "100x100" %>
<% end %>
<table>
  <tr>
    <th>
      <h4>name</h4>
    </th>
    <th>
      <h4>introduction</h4>
    </th>
    <th colspan="3"></th>
  </tr>

  <% @users.each do |user| %>
  <tr>
    <td><%= user.title %></td>
    <td><%= user.body %></td>
    <td><%= link_to user_path(user.id) %></td>
    <td><%= link_to "Edit", edit_user_path(user.id) %></td>
    <td><%= link_to "Destroy", user_path(user.id), method: :delete, "data-confirm" => "本当に消しますか?" %></td>
  </tr>
  <% end %>
</table>

<h1>New book</h1>
<% if @user.errors.any? %>
  <div id = "errors_explanation" >
    <%= pluralize(@user.errors.count, "error") %>errors prohibited this obj from being saved:
    <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>

<%= form_with model: @users, url: '/users', method: :post do |f| %>
  <h4>Title</h4>
  <%= f.text_field :title %>
  <h4>Opinion</h4>
  <%= f.text_area :body %>
  <%= f.submit 'Create Book' %>
<% end %>
